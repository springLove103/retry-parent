server:
  port: 0        # 业务端口，随意

spring:
  application:
    name: multiple-service     # 注册到 Nacos 的服务名
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.103.154:3306/dy_table?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&autoReconnect=true&zeroDateTimeBehavior=convertToNull
    username: yanfa_admin
    password: q3fqlMaj5a512fda#w6ae5
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.103.213:30669   # Nacos 地址
        namespace: public               # 命名空间 ID
        group: DEFAULT_GROUP            # 分组
        # 可选：元数据
        metadata:
          version: v1.0
          region: hz



logging:
  level:
    com.alibaba.cloud.nacos: DEBUG